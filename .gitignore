.env
__pycache__/
*.pyc
*.egg-info/
*.db
.orchestrator/*
!.orchestrator/checks/
inspect-script.log
.misc_notes/
.DS_Store

# Node / build (root playwright deps + the React pilot)
node_modules/
dist/
# Netlify drops the built bostadskalkyl app here at deploy time (see netlify.toml);
# never commit the generated bundle.
/bk-assets/

# Playwright MCP artifacts + UI assessment / pilot screenshots (not source)
.playwright-mcp/
bk-*.png
hub-*.png
react-*.png
