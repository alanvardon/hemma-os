.env
.env.*
!.env.example
__pycache__/
*.pyc
*.egg-info/
*.db
.orchestrator/*
!.orchestrator/checks/
inspect-script.log
.misc_notes/
.DS_Store

# Node / build (root playwright deps + the React pilot)
node_modules/
dist/
# Deploy-time build output (see .github/workflows/deploy.yml) — the Pages
# workflow assembles the site into _site/ and the built bostadskalkyl bundle
# into bk-assets/. Never commit the generated bundle.
/bk-assets/
/_site/

# Playwright MCP artifacts + UI assessment / pilot screenshots (not source)
.playwright-mcp/
bk-*.png
hub-*.png
react-*.png
